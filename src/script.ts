//selecting elements
const headEl = document.getElementsByClassName("head")[0];

// gelobal vraibales
let imgCount = 0;

// functions
const listImages = () => {};

// eventlisnters
window.addEventListener("lod", listImages);
